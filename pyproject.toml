[project]
name = "openskillmodel"
version = "0.1.0"
description = "Duel Channel openskill bindings"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "dacite>=1.9.2",
    "openskill>=6.2.0",
]
